<?php

namespace Tests\Unit;

use PHPUnit\Framework\TestCase;
use App\Services\TelegramBotService;

class CheckIfUserIsAdminTest extends TestCase
{
    /**
     * A basic unit test example.
     */
    public function check_if_user_is_admin_return_true(): void
    {
        // $result =  json_decode(Storage::get('TestObjects.json'), true);
        //Достаем объекты и прогоняем
        //Достаем объекты и прогоняем
        //Достаем объекты и прогоняем
        //Достаем объекты и прогоняем
        //Достаем объекты и прогоняем




        // $service = new TelegramBotService();
        // $service->checkIfUserIsAdmin();
        // $this->assertTrue(true);
        // $greeter = new Greeter;

        // $greeting = $greeter->greet('Alice');

        // $this->assertSame('Hello, Alice!', $greeting);
    }
}
